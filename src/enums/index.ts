// 窗口状态
export enum windowStateList {
  VUE = 'vueState',
  REACT = 'reactState',
  SETTINGS = 'settingsState'
}

// 设置窗口子内容
export enum settingsContent {
  THEME = 'theme',
  DOCK = 'dock',
  ABOUT = 'about'
}
